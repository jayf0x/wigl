{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability shared by every widget window (matched by glob, so a new widget's window label needs no edit here)",
  "windows": [
    "*"
  ],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-outer-position",
    "core:window:allow-set-position",
    "core:window:allow-current-monitor",
    "opener:default",
    "shell:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        { "name": "sh", "cmd": "sh", "args": true },
        { "name": "sqlite3", "cmd": "sqlite3", "args": true },
        { "name": "open", "cmd": "open", "args": true },
        {
          "name": "code",
          "cmd": "/Applications/Visual Studio Code.app/Contents/Resources/app/bin/code",
          "args": true
        },
        { "name": "github", "cmd": "/usr/local/bin/github", "args": true }
      ]
    }
  ]
}